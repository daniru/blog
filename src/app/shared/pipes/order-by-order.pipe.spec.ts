import { OrderByOrderPipe } from './order-by-order.pipe';

describe('OrderByOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
